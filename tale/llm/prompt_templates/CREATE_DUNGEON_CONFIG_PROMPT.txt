<context>{context}</context>\n[USER_START]Using the information supplied inside the <context> tags and the zone information: {zone_info}, create a dungeon configuration that would fit the zone's theme and atmosphere. The dungeon should be appropriate for the zone's level and mood. Choose creatures from the zone's races and items from the zone's items list. Fill in this JSON template and do not write anything else: {dungeon_config_template}. Write the response in valid JSON.